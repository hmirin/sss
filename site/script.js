document.querySelector('#instruction').textContent = 'Install sss by following these instructions: ' + new URL('installation.md', location.href).href;
document.querySelector('#copy').addEventListener('click',async()=>{const status=document.querySelector('#copy-status');try{await navigator.clipboard.writeText(document.querySelector('#instruction').textContent);status.textContent='Copied. Paste it into your agent.';}catch{status.textContent='Select the instruction above and copy it manually.';const range=document.createRange();range.selectNodeContents(document.querySelector('#instruction'));const selection=window.getSelection();selection.removeAllRanges();selection.addRange(range);}});
